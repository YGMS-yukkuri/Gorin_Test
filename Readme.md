# 技能五輪のB・C問題を解いていく
---
## 解いていく中で気づいた点や、わからなかったため回答を見て気付きを得た点をまとめるやつ
### 2025/12/02
#### B1
グリッドの扱い方はもとより知っていたが、テンプレートを作成する際の`repeat([回数], [サイズ])`の記法を知らなかった。また、`grr --> grid-rows`や`grc --> grid-columns`といったVSCodeで使用可能な省略記法について気付きを得られた。
#### B2
`@keyframes`を用いたCSSのみでのアニメーション作成は行ったことがなかったため、宣言方法や作り方を知った。
#### B3
`input`要素を`display:none`指定して非表示にし、`label`要素としての当たり判定を確保してチェックボックスをオンオフできるようにし、それに応じたCSSを実装するという手順が新しい考え方として得られた。また、中にある丸い部分については、あまり難しく考えずに`margin`プロパティを用いて実装することで簡単に実装できる。

また、チェックされているときにスタイルを変更するという処理は`input:checked+label`のように表記することで実装することができる。また、`label .circle`と表記することでlabelのさらに子要素についても変更させることができる。
#### B4
div要素をきれいな円状に整形し、`background-image`プロパティを用いて着色。
`conic-gradient`というグラデーションを生成する関数をimageの引数に設定し、色の範囲を設定する。このとき、色の場所(範囲)は`0 30%`のようにどこからどこまでを設定しないと、色がくっきりと変動しないためグラフにならず、ただグラデーションのかかった円になってしまう。

### 2025/12/03
#### B5
CSSにて`Active`を用いることでクリック中か否かの判定とCSSの変更ができる。
#### C1
`for(let i = ``初期値``; ``終了条件(100 >= 100など)``; ``増分``)`の記法を忘れていた
`console.log`は知っていたが、`textContent`を用いて文字列を解釈し、`appendChild`で現在の要素の一番下に追加するという処理を行えることがわからなかった。

### 2025/12/05
#### C2
`setInterval()`にてn秒に一回ループが使用できる。
#### C3
`mousemove`イベントを用いてマウスの移動を検知できることを知った。

`clinetX`や`clinetY`にて現在のカーソル座標を取得できることを知った

`.style`を用いてCSSを変更できる